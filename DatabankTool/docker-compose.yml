services:
  mongodb:
    image: mongo:7
    container_name: databank-mongo
    ports:
      - "27017:27017"
    volumes:
      - databank-data:/data/db
    restart: unless-stopped

volumes:
  databank-data:
